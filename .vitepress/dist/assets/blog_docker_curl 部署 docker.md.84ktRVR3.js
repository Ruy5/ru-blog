import{_ as e,c as s,o as a,a4 as o}from"./chunks/framework.eSehZVqP.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"blog/docker/curl 部署 docker.md","filePath":"blog/docker/curl 部署 docker.md"}'),c={name:"blog/docker/curl 部署 docker.md"},t=o(`<p>在当今的软件开发和部署中，容器技术正日益流行，而Docker作为最受欢迎和广泛应用的容器平台之一，为我们提供了便捷、灵活且高效的容器化解决方案。本篇博客将带领您在Deepin操作系统上安装和配置Docker，开启容器化世界的大门。</p><h3 id="一、安装docker" tabindex="-1">一、安装Docker <a class="header-anchor" href="#一、安装docker" aria-label="Permalink to &quot;一、安装Docker&quot;">​</a></h3><p>首先，我们需要通过几个简单的步骤来安装Docker。请按照以下指南进行操作：</p><ol><li><p>打开终端并执行以下命令来安装Docker：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>sudo curl https://get.docker.com | bash</span></span></code></pre></div></li><li><p>安装完成后，我们可以通过输入以下命令来验证Docker是否成功安装：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>sudo docker help</span></span></code></pre></div><p>如果安装成功，终端将显示<code>Usage: docker [OPTIONS] COMMAND</code>，提示您输入更详细的Docker命令。</p></li></ol><h3 id="二、配置镜像加速" tabindex="-1">二、配置镜像加速 <a class="header-anchor" href="#二、配置镜像加速" aria-label="Permalink to &quot;二、配置镜像加速&quot;">​</a></h3><p>在使用Docker时，下载镜像是一个常见的操作。然而，如果不配置镜像加速，下载速度可能会比较慢。下面是配置镜像加速的步骤：</p><ol><li><p>首先，我们需要选择一个国内镜像加速器。以下是几个常用的国内镜像加速地址：</p><ul><li>腾讯云的镜像地址：<code>https://mirror.ccs.tencentyun.com</code></li><li>网易的镜像地址：<code>http://hub-mirror.c.163.com</code></li><li>阿里云的镜像地址：<code>https://{自己的阿里云ID}.mirror.aliyuncs.com</code></li></ul></li><li><p>创建并编辑Docker的配置文件<code>daemon.json</code>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>sudo mkdir -p /etc/docker</span></span>
<span class="line"><span>sudo vim /etc/docker/daemon.json</span></span></code></pre></div></li><li><p>在打开的文件中，输入以下内容来配置镜像加速地址（以网易镜像为例）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>{</span></span>
<span class="line"><span>  &quot;registry-mirrors&quot;: [&quot;http://hub-mirror.c.163.com&quot;]</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p>保存并关闭文件。然后，重启Docker服务以使配置生效：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>sudo systemctl daemon-reload</span></span>
<span class="line"><span>sudo systemctl restart docker // 重启docker</span></span>
<span class="line"><span>sudo systemctl enable docker  // 让docker开机自启</span></span></code></pre></div></li></ol><p>现在，您已经成功安装和配置了Docker，并设置了镜像加速器，使您能够更快速地下载和使用容器镜像。</p>`,8),p=[t];function n(l,i,r,d,h,u){return a(),s("div",null,p)}const g=e(c,[["render",n]]);export{m as __pageData,g as default};
